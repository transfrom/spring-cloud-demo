server: 
  port: 3344 
  
spring:
  application:
    name:  spring-cloud-config
  cloud:
    config:
      server:
        git:
          uri: git@github.com:transfrom/spring-cloud-config.git #GitHub上面的git仓库名字
 

